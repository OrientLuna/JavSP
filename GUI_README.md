# JavSP GUI 使用说明

## 概述

JavSP 现在支持图形用户界面（GUI），提供更友好的操作体验。GUI基于tkinter开发，支持跨平台运行。

## 功能特性

### 🎯 核心功能
- **文件扫描**: 可视化扫描影片文件，显示扫描进度和结果
- **配置管理**: 图形化配置编辑器，支持所有配置项
- **增量扫描**: 文件哈希缓存，避免重复处理已扫描文件
- **实时监控**: 文件夹变化监控，自动检测新增或修改的文件
- **缓存管理**: 可视化缓存管理，支持清理、导入、导出操作

### 🔧 界面组件
- **主窗口**: 扫描控制、结果显示、状态监控
- **配置编辑器**: 分类配置界面，支持扫描器、网络、整理器等设置
- **文件扫描器**: 实时监控、缓存管理、日志显示

## 安装要求

### 基础依赖
```bash
pip install pydantic confz requests colorama tqdm Pillow pydantic-extra-types pendulum cloudscraper lxml beautifulsoup4 pretty_errors
```

### GUI依赖
- Python 3.8+
- tkinter (通常随Python安装)

#### 检查tkinter支持
```bash
python -c "import tkinter; print('tkinter可用')"
```

#### 不同平台的tkinter安装
- **Ubuntu/Debian**: `sudo apt-get install python3-tk`
- **CentOS/RHEL**: `sudo yum install tkinter`
- **Windows/macOS**: 通常已包含在Python中

## 使用方法

### 方法1: 配置文件启用GUI
1. 编辑 `config.yml` 文件
2. 设置 `general.enable_gui: true`
3. 运行 `python -m javsp`

### 方法2: 直接启动GUI
```bash
python run_gui.py
```

### 方法3: GUI模块直接运行
```bash
python javsp/gui_entry.py
```

## 界面说明

### 主窗口
- **扫描文件夹**: 选择要扫描的目录
- **开始扫描**: 启动文件扫描
- **强制重新扫描**: 忽略缓存，重新扫描所有文件
- **配置设置**: 打开配置编辑器
- **清空缓存**: 清空扫描缓存
- **结果列表**: 显示扫描到的影片信息

### 配置编辑器
分为多个标签页：
- **扫描器**: 文件夹、文件格式、扫描选项
- **网络**: 代理、超时、重试设置
- **整理器**: 运行模式、文件操作、路径设置
- **通用**: 缓存、应用设置

### 文件扫描器
- **扫描状态**: 显示扫描统计和实时日志
- **实时监控**: 监控文件夹变化
- **缓存管理**: 查看和管理缓存数据

## 增量扫描

JavSP现在支持增量扫描功能：

### 工作原理
1. **文件哈希**: 计算文件内容的MD5哈希（前1MB）
2. **缓存记录**: 记录已处理文件的哈希和相关信息
3. **智能跳过**: 后续扫描时跳过未变化的文件
4. **自动更新**: 文件修改后自动重新处理

### 配置选项
```yaml
general:
  enable_incremental_scan: true  # 启用增量扫描
  cache_file: "data/scan_cache.json"  # 缓存文件路径
  cache_cleanup_interval: 30  # 缓存清理间隔(天)
```

### 使用场景
- **软链接模式**: 解决软链接文件无法识别已处理的问题
- **大型库**: 提高重复扫描的速度
- **增量更新**: 只处理新增或修改的文件

## 故障排除

### GUI无法启动
1. **检查tkinter**: 确认tkinter已安装
2. **检查依赖**: 确认所有Python依赖已安装
3. **检查权限**: 确认有读写缓存目录的权限

### 配置不生效
1. **检查配置文件**: 确认YAML格式正确
2. **检查字段**: 确认配置字段名称正确
3. **重启应用**: 修改配置后需要重启

### 缓存问题
1. **清理缓存**: 在文件扫描器中清理缓存
2. **检查权限**: 确认有写入缓存文件的权限
3. **手动删除**: 删除 `data/scan_cache.json` 文件

## 性能优化

### 大型文件夹扫描
- 启用增量扫描
- 设置合适的文件过滤
- 调整并发扫描数量

### 内存使用
- 定期清理缓存
- 关闭不必要的监控
- 调整日志保留数量

## 技术细节

### 架构设计
- **MVC模式**: 界面与逻辑分离
- **模块化**: 各功能独立模块
- **线程安全**: 后台任务不影响界面响应

### 数据存储
- **JSON缓存**: 轻量级缓存格式
- **文件哈希**: MD5算法保证唯一性
- **增量更新**: 只记录变化信息

### 兼容性
- **向后兼容**: 不影响原有命令行功能
- **可选启用**: 通过配置开关控制
- **优雅降回**: GUI启动失败时自动回退到命令行

## 开发说明

如果需要修改或扩展GUI功能：

### 添加新功能
1. 在相应模块中添加功能
2. 更新配置类（如需要）
3. 添加界面元素
4. 测试功能完整性

### 界面风格
- 使用ttk组件保证跨平台一致性
- 遵循现有的布局规范
- 添加适当的错误处理

### 日志记录
```python
import logging
logger = logging.getLogger(__name__)
logger.info("操作信息")
logger.error("错误信息")
```

## 反馈和支持

如遇到问题或有改进建议，请：
1. 检查本文档的故障排除部分
2. 查看项目Issues页面
3. 提交新的Issue描述问题

---

*GUI功能仍在不断完善中，欢迎提供反馈和建议*